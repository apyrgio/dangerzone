name: Check github-builder

permissions:
  contents: read

on:
  push:

jobs:
  build:
    uses: docker/github-builder/.github/workflows/build.yml@aea4cccbe6c8e0a47458ef6a8fc89e6536281b29
    permissions:
      contents: read # to fetch the repository content
      id-token: write # for signing attestation(s) with GitHub OIDC Token
      packages: write # for pushing to GHCR
    with:
      output: image
      push: true
      meta-images: ghcr.io/freedomofpress/dangerzone-test-github-builder
      registry-auths: |
        - registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
